# Errors

Guardrail errors contain a stable code and array path as well as a formatted path. Default diagnostics intentionally omit input values. `format_error` offers multiline and compact views without a JSON dependency.
