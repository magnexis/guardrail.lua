# Invariants

An invariant descriptor has `check`, optional `name`, and optional `message`. `with_invariant` checks before and after the operation by default. Pass `{ before = false }` or `{ after = false }` for an explicitly one-sided check.
