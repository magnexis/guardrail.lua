# Annotations

`guardrail.annotations.generate(name, schema)` emits LuaLS/EmmyLua class fields for table schemas. The optional module keeps filesystem writing (`write_file`) outside the runtime core.
