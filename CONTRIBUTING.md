# Contributing

Run `lua spec/run.lua` before opening a change. Keep the dependency-free core compatible with Lua 5.1 syntax and add a regression test for behavioral changes. Do not add globals or expose input values in default error messages.
